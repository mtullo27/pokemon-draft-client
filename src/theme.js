import { createTheme } from "@mui/material/styles"

const theme = createTheme({
    palette: {
        primary: {
            main: "#261B39"
        },
        secondary: {
            main: "#343541"
        }
    },
    spacing: 8
})

export default theme
